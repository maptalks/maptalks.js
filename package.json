{
  "name": "gl-layer",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build-dev": "turbo run build-dev*",
    "lint": "turbo run lint",
    "prettier": "prettier --parser=typescript --write **/*.{js,ts,md}",
    "test": "turbo run test",
    "changeset": "changeset",
    "changeset-version": "changeset version",
    "release": "pnpm build && changeset publish",
    "changeset-version-snapshot": "changeset version --snaphot",
    "release-snapshot": "pnpm build && changeset publish --no-git-tag --snapshot --tag snapshot"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fuzhenn/gl-layers.git"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "@types/node": "^18.6.4",
    "cross-env": "^7.0.3",
    "eslint": "^8.57.0",
    "lint-staged": "^15.0.2",
    "prettier": "^3.2.5",
    "rimraf": "^6.0.1",
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18.16.1"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "git add"
    ],
    "*.ts?(x)": [
      "prettier --parser=typescript --write",
      "git add"
    ]
  },
  "packageManager": "pnpm@9.1.2",
  "license": "MIT"
}
