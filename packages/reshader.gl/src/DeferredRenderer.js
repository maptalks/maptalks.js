import Renderer from './Renderer.js';

/**
 * A renderer based on MTR, which needs some extensions unsupported by IE11 and EDGE
 */
class DeferredRenderer extends Renderer {

}

export default DeferredRenderer;
